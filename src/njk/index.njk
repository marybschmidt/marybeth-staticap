{% extends '_layouts/base.njk' %}

{% block page_title %}New index page title{% endblock %}

{% block content %}

<div class="container">
  <div class="row">
    <div class="col-sm-6 offset-sm-3">
    <h1>Pirates Can Blog Too</h1>
    <p class="author">By {% for author in authors %}
      <a href="{{ author.link }}" target="_blank">{{ author.name }}</a>{% if not loop.last %}{% if loop.revindex0 == 1 %} and {% else %}, {% endif %}{% endif %} {% endfor %}
      <br> Published {{ publish_date }}
    </p>
    </div>
  </div>
  <div class="row">
    <div class="col-sm-6 offset-sm-3">
      <p>
        Many believe pirates don't have a brain or a feelings. But in these blogs, pirates tell the public of the most dramatic and interesting stories on the sea. From storms to love to wrecked ships to new ships, you are in for a real treat. Argh. 
      </p>
      {% for entry in data.entries %}
        <h3 class="entry-headline"><a href="{{ entry.url }}">{{ entry.title }}</a></h3>
        <p class="entry-date">{{ entry.date }}</p><hr>
      {% endfor %}
    </div>
  </div>
</div>
{% endblock %}
